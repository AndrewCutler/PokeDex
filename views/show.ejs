<% include partials/header %>

  <h1> <%= pokemon.name %> </h1>
  <h4> Type: <%= pokemon.type %> </h4>
  <img src="<%= pokemon.image %>" alt="<%= pokemon.name %>" style="width: 400px;">
  <br/>
  <br/>
  <p><%= pokemon.description %></p>
  <div class="">
    <!-- strengths and weaknesses to go here -->
  </div>

<% include partials/footer %>